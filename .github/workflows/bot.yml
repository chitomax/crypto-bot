name: Crypto Bot

on:
  schedule:
    - cron: '0 * * * *'  # Cada hora
  workflow_dispatch:      # Manualmente

jobs:
  run-bot:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests
    
    - name: Create dummy file if missing
      run: |
        if [ ! -f bot_data.json ]; then echo '{"balance": 10000.0, "holdings": 0, "avg_price": 0, "trades": []}' > bot_data.json; fi

    - name: Run bot
      run: python bot_trading.py
    
    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add bot_data.json
        git diff --staged --quiet || git commit -m "Update bot data [skip ci]"
        git push
